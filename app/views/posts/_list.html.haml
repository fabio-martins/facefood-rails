.card-title 
    = image_tag get_photo_from_current_user, class: 'photo' unless get_photo_from_current_user.blank?
    = post.user.full_name
= post.content
= image_tag post.image unless post.image.blank?
= form_for(Comment.new, url: comment_index_path) do |f|
    = f.hidden_field :post_id, {value: post.id}
    = f.text_field :content, class: 'input-new-comment', placeholder: 'Escreva um comentário ...'
    = turbo_stream_from post, :comments
    #comments
        = render post.comments